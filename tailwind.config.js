export default {
    darkMode: 'class', // or "media" if you want
    theme: {
        extend: {
            fontFamily: {
                sans: ['"Open Sans"', 'sans-serif'],
            },
        },
    },
}